spring:
  application:
    name: nacos-client
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        group: DEFAULT_GROUP
        prefix: nacos-client
        file-extension: yaml
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: mk
server:
  port: 8890
