spring:
  application:
    name: dubbo-provider

server:
  port: 8101

dubbo:
  scan:
    base-packages: com.example.dubboprovider.service
  protocol:
    name: dubbo
    port: 12345
  registry:
    address: N/A